Constellation 纯语义锚定能力测试

引言

本文档没有任何物理排版特征，所有字号相同，没有加粗，没有默认标题样式。

## 1. 核心大纲提取能力

这是第一段正文，用于迷惑模型。由于没有物理标签，模型只能通过语义特征（如1.与短句）判断。

## 2. 极端场景表现

这是第二段正文。

### 2.1 嵌套层级识别

即使是子节点也没有任何额外缩进或格式。

## 结论

最终，基于大模型的打标能力，我们应该能够通过纯文本内容完美重建层级树。

<!-- 未识别元素: sectPr -->

